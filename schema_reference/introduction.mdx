---
title: "Aqua Protocol "
description: "Good to know, basic terminology"
---

Aqua protocol generates a json file as ouput when notarizing a file.
This JSON output is composed of two objects `revision` and `file index`.
The root JSON object is referred to as an Aqua tree.
